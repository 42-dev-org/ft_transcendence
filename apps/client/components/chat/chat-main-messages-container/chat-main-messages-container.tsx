import styles from './chat-main-messages-container.module.css'

export default function ChatMainMessagesContainer() : JSX.Element {
  return (
    <div className={`${styles['chat-messages-container']}`}>chat-main-messages-container</div>
  )
}
