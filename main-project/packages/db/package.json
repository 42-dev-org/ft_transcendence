{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@prisma/client": "5.4.2"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "build": "npx tsc",
    "test": "echo test"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "prisma": "5.4.2",
    "tsconfig": "workspace:*"
  }
}
